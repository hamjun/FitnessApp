import React from "react";
import { Text } from "react-native-elements";

const Home = () => {
  return (
    <Text h1 style={{textAlign: 'center'}}>
      Settings
    </Text>
  )
}

export default Home;
